# smartclinicmanagment
Run Quries in the SQL Server Mnagment studio
Run the backend in vs studio(replace the connection string(in appsettings json file  and context files)  with the respective server )
